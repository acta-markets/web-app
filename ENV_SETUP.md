# Environment setup

Create a `.env.local` file in the project root with:

```bash
MONGODB_URI="YOUR_MONGODB_CONNECTION_STRING"
MONGODB_DB="yuzu"
```

Then run:

```bash
npm install
npm run dev
```


